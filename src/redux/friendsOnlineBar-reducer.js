let initialState = {
    friendsOnlineData: [
        { name: 'Rodion' },
        { name: 'Valera' },
        { name: 'Oleg' }
    ]
}

const friendsOnlineBarReducer = (state = initialState, action) => {

    return state
}

export default friendsOnlineBarReducer